{
  "name": "website1",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=3010",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint .",
    "lint:fix": "eslint --ext \".js,.ts,.vue,.json\" --fix ."
  },
  "devDependencies": {
    "@nm/ui": "workspace:^",
    "@nm/tsconfig": "workspace:^",
    "nuxt": "^3.4.3"
  }
}
